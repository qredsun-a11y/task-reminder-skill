# Task Reminder Skill

## 描述
帮助用户管理每日任务，每隔1小时确认任务完成进度。
支持任务完成标记、延期处理、自动续接等功能。

## 触发关键词
- "今晚有几个任务"
- "今天有几个任务"
- "待完成任务"
- "今晚任务"
- "今天任务"

## 文件结构
```
task_reminder/
├── SKILL.md                  # 本文件
├── task_manager.py           # 任务状态管理
├── task_reminder_main.py     # 主处理逻辑
├── send_reminder.py          # 定时提醒发送脚本
├── setup_cron.py             # 定时任务设置脚本
├── tasks.json                # 任务存储文件
└── state.json                # 状态存储文件
```

## 核心功能

### 1. 任务收集
自动解析用户输入的任务列表，存储到 tasks.json。

### 2. 定时提醒
每小时发送消息，确认未完成任务进度。

### 3. 状态管理
- **完成任务**：用户回复"xxx完成"，标记为已完成
- **延期处理**：用户回复"xxx明天继续"，标记为延期，询问是否结束当天工作
- **自动续接**：第二天自动将延期任务加入新列表

### 4. 完成检查
所有任务完成后发送完成提醒。

## 使用流程

### 第一步：初始化任务
```
用户：今晚有几个任务需要完成：1. git_repo 搭建 2. 整理简历
系统：收到2个任务，我将每小时确认进度。
      1. • git_repo 搭建
      2. • 整理简历
```

### 第二步：标记完成
```
用户：git_repo 搭建完成
系统：✅ 已标记完成！还剩 1 个任务。

      📋 当前未完成任务：
      1. 整理简历
```

### 第三步：延期处理
```
用户：整理简历明天继续
系统：⏸️ 已标记延期。

      📋 当前未完成任务：（无）

      今天工作是否结束？
```

### 第四步：结束当天工作
```
用户：结束了
系统：今天工作辛苦了，先休息吧！
      明天继续的任务：
      • 整理简历
      💤
      
      [定时提醒已停止]
```

### 第五步：第二天自动续接
```
用户：今天有几个任务：1. 搭建服务器 2. 写文档
系统：收到3个任务（1个昨天延期）。
      1. • 搭建服务器
      2. • 写文档
      3. 🔄 整理简历（昨天延期）
```

## 命令参考

| 动作 | 示例消息 | 系统响应 |
|------|---------|---------|
| 创建任务 | "今晚有几个任务：1.A 2.B" | 确认收到并启动定时提醒 |
| 标记完成 | "A完成了" | 标记完成，显示剩余任务 |
| 标记延期 | "B明天继续" | 标记延期，询问是否结束 |
| 结束工作 | "结束了/下班" | 停止提醒，显示明天任务 |
| 继续工作 | "继续" | 继续定时提醒 |
| 查看进度 | （任意消息） | 显示当前进度报告 |

## 定时提醒消息格式
```
⏰ 任务进度确认时间到！

📊 任务进度：1/3 完成

✅ 已完成：
  ✓ git_repo 搭建

⏳ 未完成：
  • 整理简历
  • openclaw添加telegram渠道

请告诉我哪些任务已完成？
```

## 技术说明

- 任务数据存储在 `tasks.json`（JSON 格式）
- 状态数据存储在 `state.json`
- 定时任务使用 openclaw 的 cron 工具管理
- 每小时触发一次进度检查

## 安装

运行 `python setup_cron.py` 设置定时任务。
